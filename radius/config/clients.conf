# =============================================================================
# FreeRADIUS clients.conf – RADIUS klienti (NAS zariadenia)
# Generované automaticky z clients.conf.template
# =============================================================================

# ── UniFi Access Pointy ───────────────────────────────────────────────────────
client unifi_aps {
    ipaddr          = 0.0.0.0/0
    secret          = 6a8284911046cf0c191f0532425e28e30f501e966be7fd6f
    shortname       = unifi
    nas_type        = other
    require_message_authenticator = yes
}

# ── Loopback pre testovanie ───────────────────────────────────────────────────
client localhost {
    ipaddr          = 127.0.0.1
    secret          = 6a8284911046cf0c191f0532425e28e30f501e966be7fd6f
    shortname       = localhost
    nas_type        = other
    require_message_authenticator = yes
}

# ── Interná Docker sieť (pre testovanie z iných kontajnerov) ─────────────────
client docker_internal {
    ipaddr          = 172.18.0.0/16
    secret          = 6a8284911046cf0c191f0532425e28e30f501e966be7fd6f
    shortname       = docker
    nas_type        = other
}
